# Lo de esta clase esta en diccionario_alumnos


# DICCIONARIOS : DICT


# No estan indizados numericamente (a diferencia de las listas y las tuplas), estan indizados pero a traves de un string
# No tienen un orden
# Es una coleccion de CLAVES/VALOR
# Es como una lista de variables
# Clave -> KEY -> seria la variable
# Valor -> es el valor ---> como y = 5 ( y es la variable, y 5 es el valor)
# Podemos poner varias variables en un UNICO conjunto de datos

# Los diccionarios hay que encerrarlos entre llaves {}
    # [] -> las listas
    # () -> las tuplas
    # {} -> los sets (tambien, la diferencia es que estos tienen los valores sepadados por COMAS, y que NO se pueden repetir)
# Las VARIABLES/KEY Son siempre STRING

# Tienen la capacidad de agregar una key que no existe 
    # alumno["localidad"] = "Avellaneda"
    # alumno["legajo"] = 30000  # si existe, lo pisa

# Es MUTABLE como las listas